//Ejemplo de calculadora no necesita este archivo tener codigo pero si quieres o deceas puedes usarlo ejemplo:
/*

namespace CalculadoraApp
{
    public class Operaciones
    {
        public double RaizCuadrada(double num)
        {
            return Math.Sqrt(num);
        }

        public double Potencia(double num, double exponente)
        {
            return Math.Pow(num, exponente);
        }

        public int CalcularFactorial(int num)
        {
            if (num == 0)
                return 1;
            else
                return num * CalcularFactorial(num - 1);
        }
    }
}



*/